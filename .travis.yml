language: go

dist: bionic

go:
- 1.14.x
- 1.16.x
- 1.17.x


# After creating your SDK project from this template repository,
# remove this entire "jobs" section as this is only applicable
# to the template repo's travis build.
jobs:
  include:
    - stage: Build and run tests
    - stage: Run system test with template
      before_install:
        - echo -e "machine github.ibm.com\n  login $GITHUB_OAUTH_TOKEN" > ~/.netrc
      script:
        - git clone https://github.ibm.com/CloudEngineering/sdkgen-systest.git
        - cd sdkgen-systest
        - ./install_scripts/install_deps_ubuntu.sh
        - ./setup_and_generate.sh -l ibm-go -t $TRAVIS_BRANCH -s

notifications:
  email: true

env:
  global:
    - GO111MODULE=on

before_install:
  - sudo apt-get update
  - pyenv global 3.8

install:
  - curl -sfL https://raw.githubusercontent.com/golangci/golangci-lint/master/install.sh| sh -s -- -b $(go env GOPATH)/bin v1.41.1
  - curl -s https://codecov.io/bash > $HOME/codecov-bash.sh && chmod +x $HOME/codecov-bash.sh
  - curl -sfL https://raw.githubusercontent.com/securego/gosec/master/install.sh | sh -s -- -b $(go env GOPATH)/bin v2.8.1

script:
  - make travis-ci

# To publish code coverage information, uncomment this section.
# (requires CODECOV_TOKEN env var in travis build settings)
# after_success:
#  - build/publishCodeCoverage.sh

# To enable semantic-release, uncomment these sections.
# before_deploy:
#   - nvm install 14
#   - npm install -g npm@6.x
#   - npm install @semantic-release/changelog
#   - npm install @semantic-release/exec
#   - npm install @semantic-release/git
#   - npm install @semantic-release/github
#   - pip install --user bump2version
#
# deploy:
#   - provider: script
#     script: npx semantic-release
#     skip_cleanup: true
#     on:
#       go: '1.14.x'
#       branch: main
